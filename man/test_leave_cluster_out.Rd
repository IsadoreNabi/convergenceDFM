% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robustness_tests.R
\name{test_leave_cluster_out}
\alias{test_leave_cluster_out}
\title{Leave-cluster-out robustness of the X->Y coupling}
\usage{
test_leave_cluster_out(
  X_matrix,
  Y_matrix,
  cluster_map = NULL,
  n_clusters = 3,
  fallback = c("correlation", "com"),
  com = NULL,
  max_comp = 3,
  seed = 123,
  verbose = TRUE
)
}
\arguments{
\item{X_matrix, Y_matrix}{Numeric matrices (sectors in columns; \code{Y} carries the
sector identities used by the cluster map).}

\item{cluster_map}{Optional sector-to-cluster map (see "Cluster map"). \code{NULL}
triggers the fallback.}

\item{n_clusters}{Integer; number of clusters for the fallback (ignored when
\code{cluster_map} is supplied). Default \code{3}.}

\item{fallback}{Fallback rule when \code{cluster_map} is \code{NULL}: \code{"correlation"}
(default) or \code{"com"}.}

\item{com}{Optional per-sector organic-composition vector for \code{fallback = "com"}.}

\item{max_comp}{Maximum number of PLS components (default \code{3}).}

\item{seed}{Optional integer for reproducible component selection/diagnostics.}

\item{verbose}{Logical; print progress and diagnostic information. Default \code{TRUE}.}
}
\value{
List with components:
\describe{
\item{\code{baseline}}{Full-sample coupling.}
\item{\code{cluster_estimates}}{Named vector of leave-one-cluster coupling
estimates.}
\item{\code{cluster_members}}{Named list of the sectors in each cluster.}
\item{\code{cluster_sizes}}{Named integer vector of cluster sizes.}
\item{\code{bias},\code{se}}{Delete-a-group jackknife bias and standard error.}
\item{\code{influence}}{Per-cluster signed change \code{baseline - lco_c}.}
\item{\code{retention}}{Per-cluster \code{lco_c / baseline}.}
\item{\code{influential_clusters}}{Clusters ordered by absolute influence.}
\item{\code{n_clusters}}{Number of clusters.}
\item{\code{cluster_map}}{The resolved per-sector cluster labels.}
\item{\code{map_source}}{\code{"user"} or \code{"fallback:<method>"}.}
\item{\code{robust}}{\code{TRUE} if no single cluster changes the coupling by more
than 50 percent.}
}
}
\description{
Generalizes the delete-one-sector jackknife (\code{\link[=test_jackknife_sectors]{test_jackknife_sectors()}}) to
delete-one-\strong{cluster}: each cluster of sectors is dropped in turn from both
\code{X} and \code{Y}, and the coupling is re-estimated on the remaining sectors. Under
cross-sectional dependence of the input-output ("MIP") kind, a naive
leave-one-out is optimistic because the excluded sector's information leaks in
through intermediate demand; dropping an entire value chain forces the
prediction to rely on the general gravitation rather than on near-collinear
neighbours. This is the cross-sectional complement of the temporal
time-shift / block-bootstrap nulls already in the package
(\code{\link[=rotation_null_test]{rotation_null_test()}}, \code{\link[=test_permutation_robustness]{test_permutation_robustness()}}); it reuses the same
coupling pipeline (\code{.coupling_estimate}), it does not reimplement it.
}
\section{Cluster map (pluggable)}{

Supply \code{cluster_map} as the genuine input-output partition (a per-sector label
vector, or a named list mapping each cluster to its sector names/indices). When
it is \code{NULL}, a documented \strong{fallback} partition is built with
\code{\link[=build_cluster_map]{build_cluster_map()}} (\code{"correlation"} or \code{"com"}); the fallback is a stopgap
proxy, not a demand-linkage partition, and a message flags its use.
}

\section{Statistical layer (do not over-read)}{

\code{bias} and \code{se} are the delete-a-group (block) jackknife estimates with the
\eqn{(g-1)/g} factor over the \code{g} cluster-deletion replicates. They are
well-calibrated for roughly balanced clusters; with strongly unequal MIP
clusters they are an approximate, conservative summary, and the primary
outputs are the per-cluster \code{influence}/\code{retention} and the \code{robust} verdict.
The verdict is purely a robustness diagnostic, not a coupling point estimate.
}

\seealso{
\code{\link[=test_jackknife_sectors]{test_jackknife_sectors()}}, \code{\link[=build_cluster_map]{build_cluster_map()}}
}
