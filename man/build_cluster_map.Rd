% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robustness_tests.R
\name{build_cluster_map}
\alias{build_cluster_map}
\title{Build a fallback sector-to-cluster map}
\usage{
build_cluster_map(
  Y_matrix,
  n_clusters = 3,
  method = c("correlation", "com"),
  com = NULL
)
}
\arguments{
\item{Y_matrix}{Numeric matrix (periods in rows, sectors in columns).}

\item{n_clusters}{Integer number of clusters (clamped to \verb{1..ncol(Y_matrix)}).
Default \code{3}.}

\item{method}{Fallback rule, \code{"correlation"} (default) or \code{"com"}.}

\item{com}{Numeric vector of organic-composition values, one per sector
(length \code{ncol(Y_matrix)}); required only for \code{method = "com"}.}
}
\value{
A named integer vector of length \code{ncol(Y_matrix)}: the cluster label of
each sector, named by sector.
}
\description{
Constructs a sector-to-cluster partition for \code{\link[=test_leave_cluster_out]{test_leave_cluster_out()}} when a
genuine input-output (Leontief, "MIP") partition is not yet available. This is
an explicitly documented \strong{stopgap proxy}: the canonical clusters are value
chains defined by inter-industry demand linkages, which neither correlation
nor organic composition reproduces. Supply the real partition through the
\code{cluster_map} argument of \code{\link[=test_leave_cluster_out]{test_leave_cluster_out()}} once the MIP is at hand.
}
\section{Fallback methods}{

\describe{
\item{\code{"correlation"}}{Average-linkage hierarchical clustering on the
correlation distance \eqn{1 - \rho_{ij}} between the sectoral series
(columns of \code{Y_matrix}), cut into \code{n_clusters} groups. Sectors that move
together over time are grouped; this is a purely statistical proxy for
co-movement, not a demand-linkage cluster.}
\item{\code{"com"}}{Quantile binning of a per-sector organic-composition vector
\code{com} into \code{n_clusters} groups. Motivated by the gravitation reading
(sectors of similar organic composition share a profit-rate neighbourhood),
but again a one-dimensional proxy, not an input-output partition.}
}
}

\seealso{
\code{\link[=test_leave_cluster_out]{test_leave_cluster_out()}}
}
